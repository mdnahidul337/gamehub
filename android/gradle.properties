org.gradle.jvmargs=-Xmx1536m -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# Disable the Gradle daemon to avoid intermittent JVM/native crashes on some Windows setups.
# This makes builds a bit slower but is safer when the daemon crashes with D8/R8.
org.gradle.daemon=false
